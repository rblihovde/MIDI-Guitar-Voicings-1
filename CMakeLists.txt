cmake_minimum_required(VERSION 3.15)

project(MIDIGuitarVoicings VERSION 1.0.0)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Fetch JUCE framework
include(FetchContent)
FetchContent_Declare(
    JUCE
    GIT_REPOSITORY https://github.com/juce-framework/JUCE.git
    GIT_TAG 7.0.12
    GIT_SHALLOW TRUE
)
FetchContent_MakeAvailable(JUCE)

# Plugin configuration
juce_add_plugin(MIDIGuitarVoicings
    COMPANY_NAME "RBL Audio"
    PLUGIN_MANUFACTURER_CODE Rbla
    PLUGIN_CODE Mgvs
    FORMATS VST3 AU Standalone
    PRODUCT_NAME "MIDI Guitar Voicings"
    IS_SYNTH FALSE
    NEEDS_MIDI_INPUT TRUE
    NEEDS_MIDI_OUTPUT TRUE
    IS_MIDI_EFFECT TRUE
    EDITOR_WANTS_KEYBOARD_FOCUS FALSE
    COPY_PLUGIN_AFTER_BUILD FALSE
    VST3_CATEGORIES "Fx" "Generator" "MIDI"
    AU_MAIN_TYPE kAudioUnitType_MIDIProcessor
)

# Source files
target_sources(MIDIGuitarVoicings
    PRIVATE
        Source/PluginProcessor.cpp
        Source/PluginProcessor.h
        Source/PluginEditor.cpp
        Source/PluginEditor.h
        Source/GuitarVoicingEngine.cpp
        Source/GuitarVoicingEngine.h
        Source/PatternGenerator.cpp
        Source/PatternGenerator.h
)

# Preprocessor definitions
target_compile_definitions(MIDIGuitarVoicings
    PUBLIC
        JUCE_WEB_BROWSER=0
        JUCE_USE_CURL=0
        JUCE_VST3_CAN_REPLACE_VST2=0
        JUCE_DISPLAY_SPLASH_SCREEN=0
        JUCE_REPORT_APP_USAGE=0
)

# Link JUCE modules
target_link_libraries(MIDIGuitarVoicings
    PRIVATE
        juce::juce_audio_utils
        juce::juce_audio_processors
        juce::juce_gui_basics
    PUBLIC
        juce::juce_recommended_config_flags
        juce::juce_recommended_lto_flags
        juce::juce_recommended_warning_flags
)
